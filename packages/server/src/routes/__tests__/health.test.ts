/**
 * Health endpoint test
 */

import { describe, it, expect } from 'vitest';

describe('Health endpoint', () => {
  it('should return healthy status', () => {
    // Placeholder test — will be implemented with Fastify inject
    expect(true).toBe(true);
  });
});
